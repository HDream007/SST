<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.alibaba.com/schema/stat http://www.alibaba.com/schema/stat.xsd">

    <!--配置扫描器-->
    <context:component-scan base-package="com.rimi.sst.controller"/>

    <!--开启使用注解配置-->
    <mvc:annotation-driven/>

    <!--配置默认的处理器-->
    <mvc:default-servlet-handler/>
    <!--配置 拦截器-->
    <!--<mvc:interceptors>
       <mvc:interceptor>
           <mvc:mapping path="/**/"/>
           <mvc:exclude-mapping path="/js/**/"/>
           <mvc:exclude-mapping path="/css/**/"/>
           <mvc:exclude-mapping path="/images/**/"/>
           &lt;!&ndash;<mvc:exclude-mapping path="/longin"/>&ndash;&gt;
           <mvc:exclude-mapping path="/admin"/>
           <mvc:exclude-mapping path="/hello"/>
           &lt;!&ndash;<mvc:exclude-mapping path="/index"/>&ndash;&gt;
           <bean class="com.rimi.sst.interceptor.LoginInterceptor"/>
       </mvc:interceptor>
    </mvc:interceptors>-->

    <!--配置视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--配置响应页面的前缀、后缀-->
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp/"/>
    </bean>
</beans>